{
  "name": "react-click-to-select",
  "version": "1.0.2",
  "description": "a higher-order component that selects elements contained within it on click",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "react",
    "react-component",
    "component",
    "select",
    "text"
  ],
  "author": "Tom MacWright",
  "license": "ISC",
  "peerDependencies": {
    "react": "0.13.x || 0.14.x"
  },
  "devDependencies": {
    "react": "^0.13.0"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:mapbox/react-click-to-select.git"
  }
}
